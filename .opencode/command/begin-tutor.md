---
description: Start a tutoring session from the learning primer
---

Invoke the `begin-tutor` skill now — call `skill({ name: "begin-tutor" })` — and follow
its instructions exactly, start to finish.

Do not summarize the skill or describe what it would do. Actually run it: sync the primer,
read `primer/learning-context.md` in full, give the brief orientation, and end your message
with the single question "What are we learning today?" — then stop and wait.
